// typeof Array.from !== 'undefined'
//   ? console.log('existe')
//   : console.log('não existe')

// Liste 5 objetos nativos
//Number, Object, String, Boolean, Constructor

// Liste 5 objetos do browser
//Window, Document, history, HTMLCollection, Nodelist

// Liste 2 Métodos, Propriedades ou Objetos
// presentes no Chrome que não existem no Firefox // edge

typeof document.createCDATASection !== 'undefined'
  ? console.log('existe')
  : console.log('não existe')
